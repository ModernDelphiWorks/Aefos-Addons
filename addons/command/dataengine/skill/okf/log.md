# Log — DataEngine Specialist

2026-07-11 — **Creation** — Bundle do addon `delphi-dataengine` criado a partir do
especialista curado `delphi-dataengine-specialist.md` e transcrito do código real
do framework (`.modules\DataEngine\Source` — o DataEngine **não** possui pasta
`Examples` hoje) e do consumo vivo no ERP (backend: `Source/Core/Core.Connection.pas`,
`Source/Shared/...`). Concept docs: overview, api, idbconnection, idbdataset,
drivers, rules; playbooks: quickstart, troubleshooting. Todo fato citado por
`arquivo:linha`. Segue o padrão OKF travado pelo bundle-piloto `delphi-janus`.
